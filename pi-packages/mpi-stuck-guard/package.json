{
  "name": "mpi-stuck-guard",
  "version": "0.1.0",
  "type": "module",
  "description": "Guard against stuck sessions: block oversized recursive searches, detect repeated identical tool calls, watch provider streams for retryable timeouts, and steer the model after repeated parameter-validation failures",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
