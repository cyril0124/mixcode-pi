diff --git a/node_modules/@earendil-works/pi-tui/dist/index.d.ts b/node_modules/@earendil-works/pi-tui/dist/index.d.ts
index 50b5aeb..db86cd2 100644
--- a/node_modules/@earendil-works/pi-tui/dist/index.d.ts
+++ b/node_modules/@earendil-works/pi-tui/dist/index.d.ts
@@ -28,4 +28,6 @@ export { type Component, Container, CURSOR_MARKER, compositeTuiLine, type Focusa
 export { TuiAltScreen, type TuiAltScreenOptions } from "./tui-alt-screen.ts";
 export { TuiMainScreen, type TuiMainScreenRenderState } from "./tui-main-screen.ts";
 export { getOsc8LinkAtColumn, sliceByColumn, stripTerminalSequences, truncateToWidth, visibleWidth, wrapTextWithAnsi, } from "./utils.ts";
+export { type AltScreenSearchMatch, type AltScreenSearchSegment, findAltScreenSearchMatches, getAltScreenSearchMatchKey, } from "./alt-screen-search.ts";
+export { resolveOverlayLayout } from "./tui.ts";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@earendil-works/pi-tui/dist/index.js b/node_modules/@earendil-works/pi-tui/dist/index.js
index 97acf77..75d25d1 100644
--- a/node_modules/@earendil-works/pi-tui/dist/index.js
+++ b/node_modules/@earendil-works/pi-tui/dist/index.js
@@ -39,4 +39,7 @@ export { TuiAltScreen } from "./tui-alt-screen.js";
 export { TuiMainScreen } from "./tui-main-screen.js";
 // Utilities
 export { getOsc8LinkAtColumn, sliceByColumn, stripTerminalSequences, truncateToWidth, visibleWidth, wrapTextWithAnsi, } from "./utils.js";
+// MIXCODE_EXPORTS: upstream helpers surfaced for the MixCode host.
+export { findAltScreenSearchMatches, getAltScreenSearchMatchKey } from "./alt-screen-search.js";
+export { resolveOverlayLayout } from "./tui.js";
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/node_modules/@earendil-works/pi-tui/dist/keybindings.js b/node_modules/@earendil-works/pi-tui/dist/keybindings.js
index 3bb4668..dc0d9aa 100644
--- a/node_modules/@earendil-works/pi-tui/dist/keybindings.js
+++ b/node_modules/@earendil-works/pi-tui/dist/keybindings.js
@@ -220,13 +220,18 @@ export class KeybindingsManager {
         return resolved;
     }
 }
-let globalKeybindings = null;
+// Share keybindings across duplicate module instances (bun --compile can
+// materialize more than one copy of this module in one process). Same pattern
+// as pi-coding-agent theme (Symbol.for on globalThis).
+const GLOBAL_KEYBINDINGS_KEY = Symbol.for("@earendil-works/pi-tui:keybindings");
 export function setKeybindings(keybindings) {
-    globalKeybindings = keybindings;
+    globalThis[GLOBAL_KEYBINDINGS_KEY] = keybindings;
 }
 export function getKeybindings() {
+    let globalKeybindings = globalThis[GLOBAL_KEYBINDINGS_KEY];
     if (!globalKeybindings) {
         globalKeybindings = new KeybindingsManager(TUI_KEYBINDINGS);
+        globalThis[GLOBAL_KEYBINDINGS_KEY] = globalKeybindings;
     }
     return globalKeybindings;
 }
diff --git a/node_modules/@earendil-works/pi-tui/dist/keys.js b/node_modules/@earendil-works/pi-tui/dist/keys.js
index 46faf59..134345c 100644
--- a/node_modules/@earendil-works/pi-tui/dist/keys.js
+++ b/node_modules/@earendil-works/pi-tui/dist/keys.js
@@ -20,18 +20,27 @@
 // =============================================================================
 // Global Kitty Protocol State
 // =============================================================================
-let _kittyProtocolActive = false;
+// Share flag across duplicate module instances (bun --compile). Same pattern as
+// keybindings: Symbol.for on globalThis. Local cache keeps hot matchesKey/
+// parseKey paths as a boolean load; entry points resync from the shared slot.
+const KITTY_PROTOCOL_KEY = Symbol.for("@earendil-works/pi-tui:kitty-protocol");
+let _kittyProtocolActive = globalThis[KITTY_PROTOCOL_KEY] === true;
+function syncKittyProtocolActive() {
+    _kittyProtocolActive = globalThis[KITTY_PROTOCOL_KEY] === true;
+}
 /**
  * Set the global Kitty keyboard protocol state.
  * Called by ProcessTerminal after detecting protocol support.
  */
 export function setKittyProtocolActive(active) {
+    globalThis[KITTY_PROTOCOL_KEY] = active;
     _kittyProtocolActive = active;
 }
 /**
  * Query whether Kitty keyboard protocol is currently active.
  */
 export function isKittyProtocolActive() {
+    syncKittyProtocolActive();
     return _kittyProtocolActive;
 }
 /**
@@ -631,6 +640,7 @@ function parseKeyId(keyId) {
  * @param keyId - Key identifier (e.g., "ctrl+c", "escape", Key.ctrl("c"))
  */
 export function matchesKey(data, keyId) {
+    syncKittyProtocolActive();
     const parsed = parseKeyId(keyId);
     if (!parsed)
         return false;
@@ -997,6 +1007,7 @@ function formatParsedKey(codepoint, modifier, baseLayoutKey) {
     return formatKeyNameWithModifiers(keyName, modifier);
 }
 export function parseKey(data) {
+    syncKittyProtocolActive();
     const kitty = parseKittySequence(data);
     if (kitty) {
         return formatParsedKey(kitty.codepoint, kitty.modifier, kitty.baseLayoutKey);
diff --git a/node_modules/@earendil-works/pi-tui/dist/tui.d.ts b/node_modules/@earendil-works/pi-tui/dist/tui.d.ts
index 5b9b224..91d84eb 100644
--- a/node_modules/@earendil-works/pi-tui/dist/tui.d.ts
+++ b/node_modules/@earendil-works/pi-tui/dist/tui.d.ts
@@ -305,4 +305,10 @@ export declare abstract class TuiBase extends Container implements TUI {
         timeoutMs: number;
     }): Promise<TerminalColorScheme | undefined>;
 }
+export declare function resolveOverlayLayout(options: OverlayOptions | undefined, overlayHeight: number, termWidth: number, termHeight: number): {
+    width: number;
+    row: number;
+    col: number;
+    maxHeight?: number;
+};
 //# sourceMappingURL=tui.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@earendil-works/pi-tui/dist/tui.js b/node_modules/@earendil-works/pi-tui/dist/tui.js
index 94adbdc..f6b1931 100644
--- a/node_modules/@earendil-works/pi-tui/dist/tui.js
+++ b/node_modules/@earendil-works/pi-tui/dist/tui.js
@@ -938,4 +938,10 @@ export class TuiBase extends Container {
         });
     }
 }
+// MIXCODE_OVERLAY_LAYOUT_EXPORT: standalone overlay placement so hosts can
+// hit-test mouse input against compositor-identical geometry. Delegates to the
+// TuiBase prototype method (pure; anchor helpers live on the same prototype).
+export function resolveOverlayLayout(options, overlayHeight, termWidth, termHeight) {
+    return TuiBase.prototype.resolveOverlayLayout(options, overlayHeight, termWidth, termHeight);
+}
 //# sourceMappingURL=tui.js.map
\ No newline at end of file
diff --git a/node_modules/@earendil-works/pi-tui/dist/alt-screen-search.js b/node_modules/@earendil-works/pi-tui/dist/alt-screen-search.js
index 3437d5a..2865ace 100644
--- a/node_modules/@earendil-works/pi-tui/dist/alt-screen-search.js
+++ b/node_modules/@earendil-works/pi-tui/dist/alt-screen-search.js
@@ -44,7 +44,10 @@
     if (!normalizedQuery)
         return [];
     const corpus = buildSearchCorpus(lines);
-    const expression = new RegExp(escapeRegExp(normalizedQuery), "giu");
+    // MIXCODE_SMARTCASE: vim-style smartcase — any uppercase letter (incl.
+    // non-ASCII \p{Lu}) makes the query case-sensitive; all-lowercase stays
+    // case-insensitive.
+    const expression = new RegExp(escapeRegExp(normalizedQuery), /\p{Lu}/u.test(normalizedQuery) ? "gu" : "giu");
     const matches = [];
     for (const match of corpus.text.matchAll(expression)) {
         const start = match.index;
