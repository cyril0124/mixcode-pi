diff --git a/dist/components/markdown.js b/dist/components/markdown.js
index 5788d755400164552ed09c7d69bdd67083caa5b1..67d8f1c64772180ac8bf01152b4895bf91afba61 100644
--- a/dist/components/markdown.js
+++ b/dist/components/markdown.js
@@ -3,7 +3,15 @@ import { renderLatex } from "../latex.js";
 import { getCapabilities, hyperlink, isImageLine } from "../terminal-image.js";
 import { applyBackgroundToLine, visibleWidth, wrapTextWithAnsi } from "../utils.js";
 const STRICT_STRIKETHROUGH_REGEX = /^(~~)(?=[^\s~])((?:\\.|[^\\])*?(?:\\.|[^\s~\\]))\1(?=[^~]|$)/;
-class StrictStrikethroughTokenizer extends Tokenizer {
+class TerminalMarkdownTokenizer extends Tokenizer {
+    lheading(src) {
+        // Setext headings require an underline on a following line. Avoid the
+        // expensive heading regex when a growing paragraph has no candidate.
+        if (!/\n[ \t]*[=-]+[ \t]*(?:\n|$)/.test(src)) {
+            return undefined;
+        }
+        return super.lheading(src);
+    }
     del(src) {
         const match = STRICT_STRIKETHROUGH_REGEX.exec(src);
         if (!match) {
@@ -144,7 +152,7 @@ function trimPartialClosingFences(tokens) {
 }
 const markdownParser = new Marked();
 markdownParser.setOptions({
-    tokenizer: new StrictStrikethroughTokenizer(),
+    tokenizer: new TerminalMarkdownTokenizer(),
 });
 markdownParser.use({ extensions: [...LATEX_MARKDOWN_EXTENSIONS] });
 export class Markdown {
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 1460dd1160f1d8ccf9abf201844f7d9f01bc0fde..c7c08400f3b0531f0a970dae04147cc3a1a6695e 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -20,13 +20,15 @@ export type { EditorComponent } from "./editor-component.ts";
 export { type FuzzyMatch, fuzzyFilter, fuzzyMatch } from "./fuzzy.ts";
 export { getKeybindings, type Keybinding, type KeybindingConflict, type KeybindingDefinition, type KeybindingDefinitions, type Keybindings, type KeybindingsConfig, KeybindingsManager, setKeybindings, TUI_KEYBINDINGS, } from "./keybindings.ts";
 export { decodeKittyPrintable, isKeyRelease, isKeyRepeat, isKittyProtocolActive, Key, type KeyEventType, type KeyId, matchesKey, parseKey, setKittyProtocolActive, } from "./keys.ts";
+export { calculateScrollbarGeometry, paintScrollbarCell, type ScrollbarGeometry } from "./layout.ts";
 export { type RenderLatexOptions, renderLatex } from "./latex.ts";
 export { StdinBuffer, type StdinBufferEventMap, type StdinBufferOptions } from "./stdin-buffer.ts";
 export { ProcessTerminal, type Terminal } from "./terminal.ts";
 export { parseOsc11BackgroundColor, parseTerminalColorSchemeReport, type RgbColor, type TerminalColorScheme, } from "./terminal-colors.ts";
+export { cropKittyImageLine, getKittyImageMetadata } from "./terminal-image.ts";
 export { allocateImageId, type CellDimensions, calculateImageRows, deleteAllKittyImages, deleteKittyImage, detectCapabilities, encodeITerm2, encodeKitty, getCapabilities, getCellDimensions, getGifDimensions, getImageDimensions, getJpegDimensions, getPngDimensions, getWebpDimensions, hyperlink, type ImageDimensions, type ImageProtocol, type ImageRenderOptions, imageFallback, renderImage, resetCapabilitiesCache, setCapabilities, setCapabilityOverrides, setCellDimensions, type TerminalCapabilities, } from "./terminal-image.ts";
 export { type Component, Container, CURSOR_MARKER, compositeTuiLine, type Focusable, isFocusable, isViewportTUI, type OverlayAnchor, type OverlayBounds, type OverlayHandle, type OverlayMargin, type OverlayOptions, type OverlayUnfocusOptions, type SizeValue, type TUI, type TuiInputListener, type TuiInputListenerResult, type TuiMode, type TuiMouseButton, type TuiMouseEvent, type TuiMouseEventResult, type TuiMouseEventType, type TuiStopOptions, type ViewportTUI, } from "./tui.ts";
 export { TuiAltScreen, type TuiAltScreenOptions } from "./tui-alt-screen.ts";
 export { TuiMainScreen, type TuiMainScreenRenderState } from "./tui-main-screen.ts";
-export { getOsc8LinkAtColumn, sliceByColumn, stripTerminalSequences, truncateToWidth, visibleWidth, wrapTextWithAnsi, } from "./utils.ts";
+export { getGraphemeCellRange, getOsc8LinkAtColumn, sliceByColumn, stripTerminalSequences, truncateToWidth, visibleWidth, wrapTextWithAnsi, } from "./utils.ts";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index 2a039acd70bda559eb9397a5b6d1c1e533a4e634..dcb20a4dcc00fdc0eca9f58d0ae2d6905179b86f 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -25,6 +25,7 @@ export { fuzzyFilter, fuzzyMatch } from "./fuzzy.js";
 export { getKeybindings, KeybindingsManager, setKeybindings, TUI_KEYBINDINGS, } from "./keybindings.js";
 // Keyboard input handling
 export { decodeKittyPrintable, isKeyRelease, isKeyRepeat, isKittyProtocolActive, Key, matchesKey, parseKey, setKittyProtocolActive, } from "./keys.js";
+export { calculateScrollbarGeometry, paintScrollbarCell } from "./layout.js";
 // LaTeX rendering
 export { renderLatex } from "./latex.js";
 // Input buffering for batch splitting
@@ -33,11 +34,12 @@ export { StdinBuffer } from "./stdin-buffer.js";
 export { ProcessTerminal } from "./terminal.js";
 // Terminal colors
 export { parseOsc11BackgroundColor, parseTerminalColorSchemeReport, } from "./terminal-colors.js";
+export { cropKittyImageLine, getKittyImageMetadata } from "./terminal-image.js";
 // Terminal image support
 export { allocateImageId, calculateImageRows, deleteAllKittyImages, deleteKittyImage, detectCapabilities, encodeITerm2, encodeKitty, getCapabilities, getCellDimensions, getGifDimensions, getImageDimensions, getJpegDimensions, getPngDimensions, getWebpDimensions, hyperlink, imageFallback, renderImage, resetCapabilitiesCache, setCapabilities, setCapabilityOverrides, setCellDimensions, } from "./terminal-image.js";
 export { Container, CURSOR_MARKER, compositeTuiLine, isFocusable, isViewportTUI, } from "./tui.js";
 export { TuiAltScreen } from "./tui-alt-screen.js";
 export { TuiMainScreen } from "./tui-main-screen.js";
 // Utilities
-export { getOsc8LinkAtColumn, sliceByColumn, stripTerminalSequences, truncateToWidth, visibleWidth, wrapTextWithAnsi, } from "./utils.js";
+export { getGraphemeCellRange, getOsc8LinkAtColumn, sliceByColumn, stripTerminalSequences, truncateToWidth, visibleWidth, wrapTextWithAnsi, } from "./utils.js";
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/keybindings.js b/dist/keybindings.js
index aac8bf7598bd205e6ee5733c78e0ff5fd507341c..72ad558072abbeebae00f91172ab1f3e35440007 100644
--- a/dist/keybindings.js
+++ b/dist/keybindings.js
@@ -220,13 +220,18 @@ export class KeybindingsManager {
         return resolved;
     }
 }
-let globalKeybindings = null;
+// Share keybindings across duplicate module instances (bun --compile can
+// materialize more than one copy of this module in one process). Same pattern
+// as pi-coding-agent theme (Symbol.for on globalThis).
+const GLOBAL_KEYBINDINGS_KEY = Symbol.for("@earendil-works/pi-tui:keybindings");
 export function setKeybindings(keybindings) {
-    globalKeybindings = keybindings;
+    globalThis[GLOBAL_KEYBINDINGS_KEY] = keybindings;
 }
 export function getKeybindings() {
+    let globalKeybindings = globalThis[GLOBAL_KEYBINDINGS_KEY];
     if (!globalKeybindings) {
         globalKeybindings = new KeybindingsManager(TUI_KEYBINDINGS);
+        globalThis[GLOBAL_KEYBINDINGS_KEY] = globalKeybindings;
     }
     return globalKeybindings;
 }
diff --git a/dist/keys.js b/dist/keys.js
index 46faf595f6b007838c42fabfe2689aeb8ac7433c..134345c4d9ade944b73dd8d617cfae542eee280a 100644
--- a/dist/keys.js
+++ b/dist/keys.js
@@ -20,18 +20,27 @@
 // =============================================================================
 // Global Kitty Protocol State
 // =============================================================================
-let _kittyProtocolActive = false;
+// Share flag across duplicate module instances (bun --compile). Same pattern as
+// keybindings: Symbol.for on globalThis. Local cache keeps hot matchesKey/
+// parseKey paths as a boolean load; entry points resync from the shared slot.
+const KITTY_PROTOCOL_KEY = Symbol.for("@earendil-works/pi-tui:kitty-protocol");
+let _kittyProtocolActive = globalThis[KITTY_PROTOCOL_KEY] === true;
+function syncKittyProtocolActive() {
+    _kittyProtocolActive = globalThis[KITTY_PROTOCOL_KEY] === true;
+}
 /**
  * Set the global Kitty keyboard protocol state.
  * Called by ProcessTerminal after detecting protocol support.
  */
 export function setKittyProtocolActive(active) {
+    globalThis[KITTY_PROTOCOL_KEY] = active;
     _kittyProtocolActive = active;
 }
 /**
  * Query whether Kitty keyboard protocol is currently active.
  */
 export function isKittyProtocolActive() {
+    syncKittyProtocolActive();
     return _kittyProtocolActive;
 }
 /**
@@ -631,6 +640,7 @@ function parseKeyId(keyId) {
  * @param keyId - Key identifier (e.g., "ctrl+c", "escape", Key.ctrl("c"))
  */
 export function matchesKey(data, keyId) {
+    syncKittyProtocolActive();
     const parsed = parseKeyId(keyId);
     if (!parsed)
         return false;
@@ -997,6 +1007,7 @@ function formatParsedKey(codepoint, modifier, baseLayoutKey) {
     return formatKeyNameWithModifiers(keyName, modifier);
 }
 export function parseKey(data) {
+    syncKittyProtocolActive();
     const kitty = parseKittySequence(data);
     if (kitty) {
         return formatParsedKey(kitty.codepoint, kitty.modifier, kitty.baseLayoutKey);
diff --git a/dist/layout.d.ts b/dist/layout.d.ts
index 92b48297a2a60b14201401a26b52cb7b196a82ea..38fe9b73b8ef653ec08e609fc097a0883dd8004f 100644
--- a/dist/layout.d.ts
+++ b/dist/layout.d.ts
@@ -33,6 +33,24 @@ export interface ScrollbarGeometry {
     thumbHeight: number;
     maxScrollTop: number;
 }
+/**
+ * Calculate geometry in terminal cells without layout, visibility, or clipping checks.
+ * Heights must be nonnegative integers; scrollTop must be within 0..max(0, contentHeight - trackHeight).
+ * Nonpositive track heights return undefined; empty or fitting content fills the track.
+ */
+export declare function calculateScrollbarGeometry(options: {
+    column: number;
+    trackTop: number;
+    trackHeight: number;
+    contentHeight: number;
+    scrollTop: number;
+}): ScrollbarGeometry | undefined;
+/**
+ * Replace a zero-based terminal column with a one-cell styled replacement, slicing to totalWidth.
+ * Clear the replaced grapheme and its link; preserve neighboring styles and optionally its background.
+ * Pad missing cells with spaces. Image protocol lines are returned unchanged.
+ */
+export declare function paintScrollbarCell(line: string, column: number, totalWidth: number, replacement: string, preserveTargetBackground: boolean): string;
 export declare function getScrollbarGeometry(box: LayoutBox, includeHiddenAuto?: boolean): ScrollbarGeometry | undefined;
 export declare function renderLayoutFrame(root: Component, width: number, height: number, requestRender: () => void): LayoutFrame;
 /** Return the visual hit path from the deepest component to the layout root. */
diff --git a/dist/layout.js b/dist/layout.js
index 99485d8881b299a8a2e5dff990515ae132836daf..63ef8b10b34ab8b467ba4e6a573172fd9c80cb6b 100644
--- a/dist/layout.js
+++ b/dist/layout.js
@@ -156,7 +156,7 @@ function layoutComponent(context, component, x, y, width, height, clip) {
     }
     return box;
 }
-function replaceScrollbarCell(line, column, totalWidth, replacement, preserveTargetBackground) {
+export function paintScrollbarCell(line, column, totalWidth, replacement, preserveTargetBackground) {
     if (isImageLine(line))
         return line;
     const graphemeRange = getGraphemeCellRange(line, column);
@@ -188,19 +188,24 @@ export function getScrollbarGeometry(box, includeHiddenAuto = false) {
     const canRevealHiddenAuto = includeHiddenAuto && box.scrollView.scrollbar === "auto" && contentHeight > trackHeight;
     if (!box.scrollView.isScrollbarVisible && !canRevealHiddenAuto)
         return undefined;
+    const column = box.rect.x + box.rect.width - 1;
+    if (column < box.clip.x || column >= box.clip.x + box.clip.width)
+        return undefined;
+    return calculateScrollbarGeometry({ column, trackTop: box.rect.y, trackHeight, contentHeight, scrollTop: box.scrollView.scrollTop });
+}
+export function calculateScrollbarGeometry({ column, trackTop, trackHeight, contentHeight, scrollTop }) {
+    if (trackHeight <= 0)
+        return undefined;
     const minThumbHeight = Math.min(2, trackHeight);
     const thumbHeight = Math.max(minThumbHeight, Math.min(trackHeight, Math.round((trackHeight * trackHeight) / contentHeight)));
     const maxScrollTop = Math.max(0, contentHeight - trackHeight);
     const maxThumbTop = trackHeight - thumbHeight;
-    const thumbOffset = maxScrollTop === 0 ? 0 : Math.round((box.scrollView.scrollTop / maxScrollTop) * maxThumbTop);
-    const column = box.rect.x + box.rect.width - 1;
-    if (column < box.clip.x || column >= box.clip.x + box.clip.width)
-        return undefined;
+    const thumbOffset = maxScrollTop === 0 ? 0 : Math.round((scrollTop / maxScrollTop) * maxThumbTop);
     return {
         column,
-        trackTop: box.rect.y,
+        trackTop,
         trackHeight,
-        thumbTop: box.rect.y + thumbOffset,
+        thumbTop: trackTop + thumbOffset,
         thumbHeight,
         maxScrollTop,
     };
@@ -217,7 +222,7 @@ function paintScrollbar(box, screen, totalWidth) {
         const replacement = isThumb
             ? box.scrollView.scrollbarThumbStyle(box.scrollView.isScrollbarActive ? "█" : "┃")
             : box.scrollView.scrollbarTrackStyle("│");
-        screen[row] = replaceScrollbarCell(screen[row] ?? "", geometry.column, totalWidth, replacement, box.scrollView.scrollbar !== "always");
+        screen[row] = paintScrollbarCell(screen[row] ?? "", geometry.column, totalWidth, replacement, box.scrollView.scrollbar !== "always");
     }
 }
 function paintBox(box, screen, totalWidth) {
diff --git a/dist/terminal-image.js b/dist/terminal-image.js
index 40e1a9e1cf8b32f09d9ee5548875fa099501ab63..4bc7659afa0cb6f0748e5ef2b727f7b544586368 100644
--- a/dist/terminal-image.js
+++ b/dist/terminal-image.js
@@ -312,14 +312,22 @@ export function getKittyImagePlacement(line) {
 export function cropKittyImageLine(line, hiddenRows, visibleRows) {
     const metadata = getKittyImageMetadata(line);
     const match = /\x1b_G([^;]*);/.exec(line);
-    if (!metadata || !match || hiddenRows < 0 || hiddenRows >= metadata.rows || visibleRows <= 0)
+    if (!metadata || !match)
         return line;
-    const croppedRows = Math.min(visibleRows, metadata.rows - hiddenRows);
-    if (hiddenRows === 0 && croppedRows === metadata.rows)
+    // A parent layout may crop a placement already clipped by a child viewport.
+    // Compose with that source rectangle instead of expanding back to the original.
+    const current = Object.fromEntries(match[1].split(",").map((control) => control.split("=")));
+    const currentRows = Number(current.r ?? metadata.rows);
+    const currentY = Number(current.y ?? 0);
+    const currentHeight = Number(current.h ?? metadata.heightPx);
+    if (hiddenRows < 0 || hiddenRows >= currentRows || visibleRows <= 0)
         return line;
-    const sourceY = Math.floor((metadata.heightPx * hiddenRows) / metadata.rows);
-    const sourceEnd = Math.ceil((metadata.heightPx * (hiddenRows + croppedRows)) / metadata.rows);
-    const sourceHeight = Math.max(1, Math.min(metadata.heightPx, sourceEnd) - sourceY);
+    const croppedRows = Math.min(visibleRows, currentRows - hiddenRows);
+    if (hiddenRows === 0 && croppedRows === currentRows)
+        return line;
+    const sourceY = currentY + Math.floor((currentHeight * hiddenRows) / currentRows);
+    const sourceEnd = currentY + Math.ceil((currentHeight * (hiddenRows + croppedRows)) / currentRows);
+    const sourceHeight = Math.max(1, Math.min(currentY + currentHeight, sourceEnd) - sourceY);
     const controls = match[1].split(",").filter((control) => !/^[yhr]=/.test(control));
     controls.push(`y=${sourceY}`, `h=${sourceHeight}`, `r=${croppedRows}`);
     return `${line.slice(0, match.index)}\x1b_G${controls.join(",")};${line.slice(match.index + match[0].length)}`;
diff --git a/dist/tui-alt-screen.d.ts b/dist/tui-alt-screen.d.ts
index 4cc739e48496a2c0c05979b12443d383ea1b962d..1485408acfe9678c18d14b90b335c32c1af71b8e 100644
--- a/dist/tui-alt-screen.d.ts
+++ b/dist/tui-alt-screen.d.ts
@@ -3,8 +3,14 @@ import { type Component, TuiBase, type TuiStopOptions, VIEWPORT_TUI, type Viewpo
 export interface TuiAltScreenOptions {
     /** Number of logical lines moved for each mouse-wheel event. */
     wheelScrollLines?: number;
-    /** Capture mouse events for viewport scrolling and application-owned text selection. */
+    /** Enable terminal mouse reporting for the fullscreen viewport (default: true). */
     mouse?: boolean;
+    /**
+     * Enable built-in viewport mouse and keyboard handling (default: true).
+     * Disable when a host owns scrolling and selection. Input listeners and
+     * focused components continue to receive input through TuiBase.
+     */
+    viewportInput?: boolean;
     /** Style a non-current transcript search match. */
     searchMatchStyle?: (text: string) => string;
     /** Style the current transcript search match. */
diff --git a/dist/tui-alt-screen.js b/dist/tui-alt-screen.js
index 08dc17e0c44140a5ee5ca7ceac0cd58824e63435..8249d26347252eb97b1d79270a783b7dee08aa2a 100644
--- a/dist/tui-alt-screen.js
+++ b/dist/tui-alt-screen.js
@@ -19,7 +19,10 @@ const FOCUS_IN = "\x1b[I";
 const FOCUS_OUT = "\x1b[O";
 const BEGIN_SYNCHRONIZED_OUTPUT = "\x1b[?2026h";
 const END_SYNCHRONIZED_OUTPUT = "\x1b[?2026l";
-const OSC133_ZONE_PREFIX = /^(?:\x1b\]133;[ABC](?:\x07|\x1b\\))+/;
+const RESET_VIEWPORT = "\x1b[?6l\x1b[r";
+// Prompt zones belong to document navigation. Replaying them while painting can
+// move the terminal cursor; selection slices may put them inside styled text.
+const OSC133_ZONE_MARKER = /\x1b\]133;[ABC](?:\x07|\x1b\\)/g;
 const OSC133_PROMPT_START = /^\x1b\]133;A(?:\x07|\x1b\\)/;
 const PAGE_SCROLL_OVERLAP = 4;
 const ALT_WHEEL_SCROLL_MULTIPLIER = 5;
@@ -100,7 +103,9 @@ export class TuiAltScreen extends TuiBase {
         this.onRightClickPaste = options.onRightClickPaste;
         this.copyOnSelect = options.copyOnSelect ?? true;
         this.copySelection = options.copySelection;
-        this.addInputListener((data) => this.handleViewportInput(data));
+        if (options.viewportInput !== false) {
+            this.addInputListener((data) => this.handleViewportInput(data));
+        }
     }
     get viewportTop() {
         return this.getPrimaryScrollView().scrollTop;
@@ -201,7 +206,7 @@ export class TuiAltScreen extends TuiBase {
         }
         else {
             const width = Math.max(1, this.terminal.columns);
-            const documentLines = this.render(width).map((line) => line.replace(OSC133_ZONE_PREFIX, ""));
+            const documentLines = this.render(width).map((line) => line.replace(OSC133_ZONE_MARKER, ""));
             this.lastDocument = this.applyLineResets(documentLines.map((line) => line.replaceAll(CURSOR_MARKER, ""))).map((line) => (isImageLine(line) || visibleWidth(line) <= width ? line : sliceByColumn(line, 0, width, true)));
             let buffer = `${BEGIN_SYNCHRONIZED_OUTPUT}${EXIT_ALT_SCREEN}${DISABLE_AUTOWRAP}`;
             for (let row = 0; row < this.lastDocument.length; row++) {
@@ -1433,7 +1438,7 @@ export class TuiAltScreen extends TuiBase {
         if (this.refreshSearch(nextLayout)) {
             nextLayout = renderLayoutFrame(root, width, height, () => this.requestRender());
         }
-        let screen = nextLayout.lines.map((line) => line.replace(OSC133_ZONE_PREFIX, ""));
+        let screen = [...nextLayout.lines];
         screen = this.applySearchHighlights(screen, nextLayout);
         screen = this.compositeScrollToEndIndicator(screen, nextLayout, width);
         screen = this.compositeOverlays(screen, width, height);
@@ -1443,6 +1448,7 @@ export class TuiAltScreen extends TuiBase {
         screen = this.compositeFlashes(screen, width, height);
         const cursorPos = this.extractCursorPosition(screen, height);
         screen = this.applyLineResets(screen).map((line) => {
+            line = line.replace(OSC133_ZONE_MARKER, "");
             if (isImageLine(line) || visibleWidth(line) <= width)
                 return line;
             return sliceByColumn(line, 0, width, true);
@@ -1454,7 +1460,8 @@ export class TuiAltScreen extends TuiBase {
         const preparedKittyScreen = redrawImages && this.imageProtocol === "kitty"
             ? this.prepareKittyScreen(screen)
             : { lines: screen, evictedImageDeletion: "" };
-        let buffer = BEGIN_SYNCHRONIZED_OUTPUT;
+        // Absolute row addresses require a full-screen origin and scrolling region.
+        let buffer = BEGIN_SYNCHRONIZED_OUTPUT + RESET_VIEWPORT;
         if (fullRedraw) {
             this.fullRedrawCount += 1;
             const clearImages = this.imageProtocol === "kitty" && hadUploadedKittyImages
diff --git a/dist/tui.js b/dist/tui.js
index 887f5e8bd3c3ba9b467f9f6c6b5eb4af8a63f7f3..61d9bab9cd3e7a77036763e09e0f80385dc23702 100644
--- a/dist/tui.js
+++ b/dist/tui.js
@@ -536,6 +536,13 @@ export class TuiBase extends Container {
     }
     start() {
         this.stopped = false;
+        // MIXCODE_RENDER_RESUME: a requestRender() that was pending at stop()
+        // time leaves renderRequested dangling (scheduleRender bails on
+        // `stopped`), so every later non-force requestRender() no-ops and
+        // interval-driven redraws (tab shimmer, loading spinner) freeze until
+        // a keypress heals the flag via requestImmediateRender(). Clear it
+        // before the requestRender() below so the handoff resumes rendering.
+        this.renderRequested = false;
         this.beforeTerminalStart();
         this.terminal.start((data) => this.handleTerminalInput(data), () => this.requestRender());
         this.afterTerminalStart();
diff --git a/dist/utils.js b/dist/utils.js
index 7b148e9e3efb6c49611be7bf416cb9a4e7d2f663..4725da7966a5c8eb0c10a0a196b0a116231dd8f6 100644
--- a/dist/utils.js
+++ b/dist/utils.js
@@ -712,7 +712,10 @@ function splitIntoTokensWithAnsi(text) {
         while (end < text.length && !extractAnsiCode(text, end)) {
             end++;
         }
-        for (const { segment } of graphemeSegmenter.segment(text.slice(i, end))) {
+        const chunk = text.slice(i, end);
+        const segments = isPrintableAscii(chunk) ? chunk : graphemeSegmenter.segment(chunk);
+        for (const item of segments) {
+            const segment = typeof item === "string" ? item : item.segment;
             const segmentIsSpace = segment === " ";
             if (!segmentIsSpace && cjkBreakRegex.test(segment)) {
                 flushCurrent();
