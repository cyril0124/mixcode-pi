diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index 5620ac326bd3885819f4dbca401522c3058a640b..fafb2fc8feea493b8d4490599ef9e652a8d017ec 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -501,6 +501,33 @@ export declare class AgentSession {
     /** Get pending steering messages (read-only) */
     getSteeringMessages(): readonly string[];
     /** Get pending follow-up messages (read-only) */
+    /**
+     * MIXCODE_QUEUE_DEQUEUE: remove specific queued messages, matched by exact
+     * text (last occurrence first), from both the session text mirror and the
+     * agent pending queue. Returns removed entries in queue order for restore.
+     */
+    removeQueuedMessages(kind: "steering" | "followUp", texts: readonly string[]): Array<{
+        message: AgentMessage;
+        text: string;
+    }>;
+    /** MIXCODE_QUEUE_DEQUEUE: re-append entries removed by removeQueuedMessages. */
+    restoreQueuedMessages(kind: "steering" | "followUp", removed: ReadonlyArray<{
+        message: AgentMessage;
+        text: string;
+    }>): void;
+    /**
+     * MIXCODE_SYSTEM_PROMPT_HOOK: install a host assembler that receives the
+     * collected BuildSystemPromptOptions and returns ordered transcript sections
+     * plus the options to publish to extensions. Keep section keys and their order
+     * stable across calls; use empty strings for temporarily empty slots. Section
+     * text excludes separators, which Pi renders between nonempty sections.
+     * The effective session prompt updates immediately; transcripts receive only
+     * changed sections on the next request. Forced extension prompts still win.
+     */
+    setSystemPromptAssembler(assembler: (options: import("./system-prompt.ts").BuildSystemPromptOptions) => {
+        sections: Record<string, string>;
+        options: import("./system-prompt.ts").BuildSystemPromptOptions;
+    }): void;
     getFollowUpMessages(): readonly string[];
     get resourceLoader(): ResourceLoader;
     /**
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 36fdb31c121d1121cebc51c6f0d61d09cf07bae0..61491c41ccd21f1e4bcdda7248765ba3cdfdb4b7 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -14,7 +14,7 @@
  */
 import { readFileSync } from "node:fs";
 import { basename, dirname } from "node:path";
-import { contentText, getCurrentSystemMessage, retryDelayMs } from "@earendil-works/pi-ai";
+import { contentText, getCurrentSystemMessage, getSystemMessageText, retryDelayMs } from "@earendil-works/pi-ai";
 import { clampThinkingLevel, cleanupSessionResources, getSupportedThinkingLevels, isContextOverflow, isRecoverableLength, isRetryableAssistantError, modelsAreEqual, resetApiProviders, streamSimple, } from "@earendil-works/pi-ai/compat";
 import { getThemeByName, theme } from "../modes/interactive/theme/theme.js";
 import { stripFrontmatter } from "../utils/frontmatter.js";
@@ -877,7 +877,25 @@ export class AgentSession {
     }
     /** Current effective system prompt, including changes not yet sent to the model. */
     get systemPrompt() {
-        return buildSystemPrompt(this._runSystemPromptOptions ?? this._baseSystemPromptOptions);
+        return this._renderSystemPrompt(this._runSystemPromptOptions ?? this._baseSystemPromptOptions);
+    }
+    // MIXCODE_SYSTEM_PROMPT_HOOK: render from collected options at every public boundary,
+    // including extension hooks and mid-run tool changes. Forced extension prompts win.
+    _renderSystemPrompt(options) {
+        if (options.forceSystemPrompt !== undefined)
+            return options.forceSystemPrompt;
+        if (!this._systemPromptAssembler)
+            return buildSystemPrompt(options);
+        return getSystemMessageText({ role: "system", content: "", sections: this._assembleSystemPromptSections(options), timestamp: 0 });
+    }
+    _assembleSystemPromptSections(options) {
+        const assembled = this._systemPromptAssembler(options);
+        Object.assign(options, normalizeBuildSystemPromptOptions(assembled.options));
+        return assembled.sections;
+    }
+    setSystemPromptAssembler(assembler) {
+        this._systemPromptAssembler = assembler;
+        this._rebuildSystemPrompt(this.getActiveToolNames());
     }
     /** Current retry attempt (0 if not retrying) */
     get retryAttempt() {
@@ -1028,7 +1046,10 @@ export class AgentSession {
             const tool = this._toolRegistry.get(name);
             return tool ? [tool] : [];
         });
-        const sections = diffSystemPromptSections(getCurrentSystemMessage(messages)?.sections ?? {}, buildSystemPromptSections(options));
+        const promptSections = this._systemPromptAssembler
+            ? this._assembleSystemPromptSections({ ...options, forceSystemPrompt: undefined })
+            : buildSystemPromptSections(options);
+        const sections = diffSystemPromptSections(getCurrentSystemMessage(messages)?.sections ?? {}, promptSections);
         return sections ? { role: "system", content: "", sections, timestamp: Date.now() } : undefined;
     }
     /**
@@ -1280,7 +1301,7 @@ export class AgentSession {
             // Emit before_agent_start before normalizing images so extension-driven model
             // selection determines the resize profile used for the request and history.
             const selectedToolsBefore = this._baseSystemPromptOptions.selectedTools;
-            const result = await this._extensionRunner.emitBeforeAgentStart(expandedText, currentImages, this._baseSystemPromptOptions);
+            const result = await this._extensionRunner.emitBeforeAgentStart(expandedText, currentImages, this._baseSystemPromptOptions, (options) => this._renderSystemPrompt(options));
             // Handlers may edit event.systemPromptOptions.selectedTools or call setActiveTools(),
             // which updates the live loadout instead. An explicit edit wins; otherwise the live
             // loadout is authoritative, so a setActiveTools() call is not undone here.
@@ -1587,6 +1608,43 @@ export class AgentSession {
         this._emitQueueUpdate();
         return { steering, followUp };
     }
+    /**
+     * MIXCODE_QUEUE_DEQUEUE: remove specific queued messages, matched by exact
+     * text (last occurrence first), from both the session text mirror and the
+     * agent pending queue. Returns removed entries in queue order for restore.
+     */
+    removeQueuedMessages(kind, texts) {
+        const tracked = kind === "steering" ? this._steeringMessages : this._followUpMessages;
+        const queue = kind === "steering" ? this.agent.steeringQueue : this.agent.followUpQueue;
+        const removed = [];
+        for (let requestedIndex = texts.length - 1; requestedIndex >= 0; requestedIndex -= 1) {
+            const text = texts[requestedIndex];
+            const trackedIndex = tracked.lastIndexOf(text);
+            if (trackedIndex === -1)
+                continue;
+            tracked.splice(trackedIndex, 1);
+            const pendingIndex = findQueuedUserMessageFromEnd(queue.messages, text);
+            if (pendingIndex === -1)
+                continue;
+            removed.push({ index: pendingIndex, message: queue.messages.splice(pendingIndex, 1)[0], text });
+        }
+        this._emitQueueUpdate();
+        return removed
+            .sort((left, right) => left.index - right.index)
+            .map(({ message, text }) => ({ message, text }));
+    }
+    /** MIXCODE_QUEUE_DEQUEUE: re-append entries removed by removeQueuedMessages. */
+    restoreQueuedMessages(kind, removed) {
+        if (removed.length === 0)
+            return;
+        const tracked = kind === "steering" ? this._steeringMessages : this._followUpMessages;
+        const queue = kind === "steering" ? this.agent.steeringQueue : this.agent.followUpQueue;
+        for (const item of removed) {
+            tracked.push(item.text);
+            queue.enqueue(item.message);
+        }
+        this._emitQueueUpdate();
+    }
     /** Number of pending messages (includes both steering and follow-up) */
     get pendingMessageCount() {
         return this._steeringMessages.length + this._followUpMessages.length;
@@ -2495,13 +2553,29 @@ export class AgentSession {
         const excludedToolNames = this._excludedToolNames;
         const isAllowedTool = (name) => (!allowedToolNames || allowedToolNames.has(name)) && !excludedToolNames?.has(name);
         const registeredTools = this._extensionRunner.getAllRegisteredTools();
+        // MIXCODE_TOOL_PRECEDENCE: host customTools before extension-registered
+        // tools so extensions own colliding tool names (builtin < sdk < extension).
         const allCustomTools = [
-            ...registeredTools,
             ...this._customTools.map((definition) => ({
                 definition,
                 sourceInfo: createSyntheticSourceInfo(`<sdk:${definition.name}>`, { source: "sdk" }),
             })),
+            ...registeredTools,
         ].filter((tool) => isAllowedTool(tool.definition.name));
+        // MIXCODE_SPAWN_ENV_BRACKET: an sdk customTool may declare spawnEnvBracket()
+        // — a per-spawn process.env contribution (undefined value = ensure-unset).
+        // Every same-name candidate is wrapped so whichever wins the merge below
+        // (the sdk tool itself, or an extension override such as a display
+        // extension re-registering "bash") still applies that env around execute.
+        const spawnEnvBrackets = new Map(this._customTools
+            .filter((tool) => typeof tool.spawnEnvBracket === "function")
+            .map((tool) => [tool.name, tool.spawnEnvBracket]));
+        const bracketedCustomTools = allCustomTools.map((tool) => {
+            const spawnEnvBracket = spawnEnvBrackets.get(tool.definition.name);
+            if (!spawnEnvBracket)
+                return tool;
+            return { ...tool, definition: wrapDefinitionWithSpawnEnvBracket(tool.definition, spawnEnvBracket) };
+        });
         const definitionRegistry = new Map(Array.from(this._baseToolDefinitions.entries())
             .filter(([name]) => isAllowedTool(name))
             .map(([name, definition]) => [
@@ -2511,7 +2585,7 @@ export class AgentSession {
                 sourceInfo: createSyntheticSourceInfo(`<builtin:${name}>`, { source: "builtin" }),
             },
         ]));
-        for (const tool of allCustomTools) {
+        for (const tool of bracketedCustomTools) {
             definitionRegistry.set(tool.definition.name, {
                 definition: tool.definition,
                 sourceInfo: tool.sourceInfo,
@@ -2531,7 +2605,7 @@ export class AgentSession {
         })
             .filter((entry) => entry !== undefined));
         const runner = this._extensionRunner;
-        const wrappedExtensionTools = wrapRegisteredTools(allCustomTools, runner);
+        const wrappedExtensionTools = wrapRegisteredTools(bracketedCustomTools, runner);
         const wrappedBuiltInTools = wrapRegisteredTools(Array.from(this._baseToolDefinitions.values())
             .filter((definition) => isAllowedTool(definition.name))
             .map((definition) => ({
@@ -3236,4 +3310,53 @@ export class AgentSession {
         return this._extensionRunner;
     }
 }
+// MIXCODE_QUEUE_DEQUEUE helpers: match queued user messages by joined text blocks.
+function findQueuedUserMessageFromEnd(messages, text) {
+    for (let index = messages.length - 1; index >= 0; index -= 1) {
+        const message = messages[index];
+        if (message.role === "user" && queuedUserText(message) === text)
+            return index;
+    }
+    return -1;
+}
+function queuedUserText(message) {
+    if (typeof message.content === "string")
+        return message.content;
+    return message.content
+        .filter((block) => block.type === "text")
+        .map((block) => block.text)
+        .join("\n");
+}
+// MIXCODE_SPAWN_ENV_BRACKET helper: apply the env contribution, invoke execute,
+// and restore process.env in `finally` — synchronously after execute returns
+// its promise. Bash-style tools snapshot process.env (getShellEnv) in their
+// synchronous prefix, so a late (post-await) snapshot only ever sees restored
+// values: fail-absent, never another tab's values.
+function wrapDefinitionWithSpawnEnvBracket(definition, spawnEnvBracket) {
+    return {
+        ...definition,
+        execute(toolCallId, params, signal, onUpdate, ctx) {
+            const contribution = spawnEnvBracket();
+            const saved = new Map();
+            for (const [key, value] of Object.entries(contribution)) {
+                saved.set(key, process.env[key]);
+                if (value === undefined)
+                    delete process.env[key];
+                else
+                    process.env[key] = value;
+            }
+            try {
+                return definition.execute(toolCallId, params, signal, onUpdate, ctx);
+            }
+            finally {
+                for (const [key, value] of saved) {
+                    if (value === undefined)
+                        delete process.env[key];
+                    else
+                        process.env[key] = value;
+                }
+            }
+        },
+    };
+}
 //# sourceMappingURL=agent-session.js.map
\ No newline at end of file
diff --git a/dist/core/extensions/loader.js b/dist/core/extensions/loader.js
index be748bf45bea69461ef4f2a9a4dfc99bb3cbb1b1..0140819423f623d9cb2ce819aefe2520b3b3beda 100644
--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -85,8 +85,18 @@ function getAliases() {
 let extensionCacheCwd;
 let extensionCacheGeneration = 0;
 const extensionCache = new Map();
+// MIXCODE_EXTENSION_SINGLE_FLIGHT: concurrent loaders (one services build per
+// tab at boot) race on the same extension paths; without sharing in-flight
+// imports every tab misses the cache window and re-imports every module
+// (measured: 13 tabs × 34 paths = 442 jiti imports, ~35s of duplicated work).
+// Only cache-token callers join/register: loadExtensions() (no token) keeps
+// upstream fresh-evaluation semantics. Cleared together with the factory cache
+// so a clear still forces a re-import; deletes are identity-checked so a stale
+// completion cannot evict a newer in-flight entry.
+const inflightExtensionModules = new Map();
 export function clearExtensionCache() {
     extensionCache.clear();
+    inflightExtensionModules.clear();
     extensionCacheCwd = undefined;
     extensionCacheGeneration++;
 }
@@ -397,7 +407,22 @@ async function loadExtensionModule(extensionPath, cacheToken) {
         if (cachedFactory) {
             return cachedFactory;
         }
+        const inflight = inflightExtensionModules.get(extensionPath);
+        if (inflight) {
+            return inflight;
+        }
+    }
+    const importPromise = importExtensionModuleUncached(extensionPath, cacheToken).finally(() => {
+        if (inflightExtensionModules.get(extensionPath) === importPromise) {
+            inflightExtensionModules.delete(extensionPath);
+        }
+    });
+    if (isCurrentCacheToken(cacheToken)) {
+        inflightExtensionModules.set(extensionPath, importPromise);
     }
+    return importPromise;
+}
+async function importExtensionModuleUncached(extensionPath, cacheToken) {
     const createJitiImpl = await getCreateJiti();
     // Compiled binaries and the bundled Node distribution use embedded modules.
     // Source TypeScript reuses host modules and root tsconfig paths. Unbundled
diff --git a/dist/core/extensions/runner.d.ts b/dist/core/extensions/runner.d.ts
index 17cf9371a0e5dc6820f1cb57f5e589cedf3bdfa8..89a8e5d0f597646f3bac18d3d0fee8c038df4d1d 100644
--- a/dist/core/extensions/runner.d.ts
+++ b/dist/core/extensions/runner.d.ts
@@ -172,7 +172,7 @@ export declare class ExtensionRunner {
     emitContext(messages: AgentMessage[]): Promise<AgentMessage[]>;
     emitBeforeProviderRequest(payload: unknown): Promise<unknown>;
     emitBeforeProviderHeaders(headers: ProviderHeaders): Promise<ProviderHeaders>;
-    emitBeforeAgentStart(prompt: string, images: ImageContent[] | undefined, systemPromptOptions: BuildSystemPromptOptions): Promise<BeforeAgentStartCombinedResult>;
+    emitBeforeAgentStart(prompt: string, images: ImageContent[] | undefined, systemPromptOptions: BuildSystemPromptOptions, renderSystemPrompt?: (options: NormalizedBuildSystemPromptOptions) => string): Promise<BeforeAgentStartCombinedResult>;
     emitResourcesDiscover(cwd: string, reason: ResourcesDiscoverEvent["reason"]): Promise<{
         skillPaths: Array<{
             path: string;
diff --git a/dist/core/extensions/runner.js b/dist/core/extensions/runner.js
index b8c238f303814fc0e38d9ba986aa74d7113ccad4..cedbb8bd134d81b7c9692b6b784ec06ecb89adca 100644
--- a/dist/core/extensions/runner.js
+++ b/dist/core/extensions/runner.js
@@ -1013,9 +1013,9 @@ export class ExtensionRunner {
         }
         return headers;
     }
-    async emitBeforeAgentStart(prompt, images, systemPromptOptions) {
+    async emitBeforeAgentStart(prompt, images, systemPromptOptions, renderSystemPrompt = buildSystemPrompt) {
         const currentOptions = normalizeBuildSystemPromptOptions(systemPromptOptions);
-        const renderCurrentSystemPrompt = () => buildSystemPrompt(currentOptions);
+        const renderCurrentSystemPrompt = () => renderSystemPrompt(currentOptions);
         const ctx = Object.defineProperties({}, Object.getOwnPropertyDescriptors(this.createContext()));
         ctx.getSystemPrompt = () => {
             this.assertActive();
diff --git a/dist/core/extensions/virtual-modules.js b/dist/core/extensions/virtual-modules.js
index 77b6d6d2627037b93d8eb0b9f7c2369ddffc1cbf..6dec74da0297198c077307d92edf0b2a0bfa78e5 100644
--- a/dist/core/extensions/virtual-modules.js
+++ b/dist/core/extensions/virtual-modules.js
@@ -35,4 +35,14 @@ export const VIRTUAL_MODULES = {
     "@mariozechner/pi-ai/providers/all": bundledPiAiProviders,
     "@mariozechner/pi-coding-agent": bundledPiCodingAgent,
 };
+// MIXCODE_TYPEBOX_FIX: keep Type bound to the bundled TypeBox module in compiled hosts.
+for (const key of ["@earendil-works/pi-ai", "@earendil-works/pi-ai/compat", "@mariozechner/pi-ai", "@mariozechner/pi-ai/compat"]) {
+    const mod = VIRTUAL_MODULES[key];
+    VIRTUAL_MODULES[key] = new Proxy(mod, {
+        get(target, prop, receiver) {
+            if (prop === "Type") return VIRTUAL_MODULES.typebox.Type ?? VIRTUAL_MODULES.typebox;
+            return Reflect.get(target, prop, receiver);
+        },
+    });
+}
 //# sourceMappingURL=virtual-modules.js.map
\ No newline at end of file
diff --git a/dist/core/footer-data-provider.js b/dist/core/footer-data-provider.js
index efc80fec6699a530d6f4ceeaef974410092e64da..4f6b043428c686c82c24402324a02f63a55548f7 100644
--- a/dist/core/footer-data-provider.js
+++ b/dist/core/footer-data-provider.js
@@ -273,65 +273,52 @@ export class FooterDataProvider {
             this.gitWatcherRetryTimer = null;
             this.setupGitWatcher();
         }, FS_WATCH_RETRY_DELAY_MS);
+        // MIXCODE_UNREF_GIT_TIMERS: a pending retry must not keep an otherwise
+        // idle process (tests, one-shot CLI commands) alive for the retry delay.
+        this.gitWatcherRetryTimer.unref?.();
     }
     handleGitWatcherError() {
         this.clearGitWatchers();
         this.scheduleGitWatcherRetry();
     }
     setupGitWatcher() {
+        // MIXCODE_POLL_ONLY_GIT_WATCH: stat-poll instead of fs.watch. inotify
+        // instances are a scarce per-user kernel quota on shared boxes
+        // (fs.inotify.max_user_instances); exhaustion makes fs.watch fail with
+        // ENOSPC and branch data silently freeze. watchFile stat-polls by path,
+        // needs no inotify, and survives atomic rename-over of HEAD. Worst-case
+        // latency: 1s poll + 500ms debounce, fine for a footer badge.
         this.clearGitWatchers();
         if (!this.gitPaths)
             return;
-        const pollGitHead = shouldPollGitHead(this.gitPaths.repoDir);
-        // Watch the directory containing HEAD, not HEAD itself.
-        // Git uses atomic writes (write temp, rename over HEAD), which changes the inode.
-        // fs.watch on a file stops working after the inode changes.
-        this.headWatcher = watchWithErrorHandler(dirname(this.gitPaths.headPath), (_eventType, filename) => {
-            if (!filename || filename === "HEAD") {
+        this.headWatchFilePath = this.gitPaths.headPath;
+        this.headWatchFileListener = (current, previous) => {
+            if (current.mtimeMs !== previous.mtimeMs ||
+                current.ctimeMs !== previous.ctimeMs ||
+                current.size !== previous.size) {
                 this.scheduleRefresh();
             }
-        }, () => this.handleGitWatcherError());
-        if (pollGitHead) {
-            this.headWatchFilePath = this.gitPaths.headPath;
-            this.headWatchFileListener = (current, previous) => {
-                if (current.mtimeMs !== previous.mtimeMs ||
-                    current.ctimeMs !== previous.ctimeMs ||
-                    current.size !== previous.size) {
-                    this.scheduleRefresh();
-                }
-            };
-            watchFile(this.headWatchFilePath, { interval: 1000 }, this.headWatchFileListener);
-        }
-        if (!this.headWatcher && !pollGitHead) {
-            return;
-        }
-        // In reftable repos, branch switches update files in the reftable directory
-        // instead of HEAD. Watch it separately so the footer picks up those changes.
+        };
+        // MIXCODE_UNREF_GIT_TIMERS: stat-poll watchers are ref'd by default and
+        // would pin the event loop forever; providers created by one-shot
+        // renders (gitBranchForWorkdir from tests/CLI) must not leak an immortal
+        // 1s poll timer. Unref'd watchers still fire while the process lives.
+        const headWatcher = watchFile(this.headWatchFilePath, { interval: 1000 }, this.headWatchFileListener);
+        headWatcher.unref?.();
+        // In reftable repos, branch switches update reftable/tables.list instead
+        // of HEAD. Poll it too; watchFile also fires when the file first appears.
         const reftableDir = join(this.gitPaths.commonGitDir, "reftable");
         if (existsSync(reftableDir)) {
-            this.reftableWatcher = watchWithErrorHandler(reftableDir, () => {
-                this.scheduleRefresh();
-            }, () => this.handleGitWatcherError());
-            if (!this.reftableWatcher) {
-                return;
-            }
             const tablesListPath = join(reftableDir, "tables.list");
-            if (existsSync(tablesListPath)) {
-                this.reftableTablesListPath = tablesListPath;
-                this.reftableTablesListWatcher = watchWithErrorHandler(tablesListPath, () => {
+            this.reftableTablesListPath = tablesListPath;
+            const reftableWatcher = watchFile(tablesListPath, { interval: 250 }, (current, previous) => {
+                if (current.mtimeMs !== previous.mtimeMs ||
+                    current.ctimeMs !== previous.ctimeMs ||
+                    current.size !== previous.size) {
                     this.scheduleRefresh();
-                }, () => this.handleGitWatcherError());
-                if (!this.reftableTablesListWatcher) {
-                    return;
                 }
-                watchFile(tablesListPath, { interval: 250 }, (current, previous) => {
-                    if (current.mtimeMs !== previous.mtimeMs ||
-                        current.ctimeMs !== previous.ctimeMs ||
-                        current.size !== previous.size) {
-                        this.scheduleRefresh();
-                    }
-                });
-            }
+            });
+            reftableWatcher.unref?.();
         }
     }
 }
diff --git a/dist/core/keybindings.d.ts b/dist/core/keybindings.d.ts
index 08e03f2a4ccf493895a7ee67e5f9eae9f27df02c..862e588bf63a0264db91026b01117851cfa2bd45 100644
--- a/dist/core/keybindings.d.ts
+++ b/dist/core/keybindings.d.ts
@@ -425,4 +425,5 @@ export declare class KeybindingsManager extends TuiKeybindingsManager {
     private static loadFromFile;
 }
 export type { Keybinding, KeyId, KeybindingsConfig };
+export declare function loadKeybindingsConfigFile(path: string): KeybindingsConfig;
 //# sourceMappingURL=keybindings.d.ts.map
\ No newline at end of file
diff --git a/dist/core/keybindings.js b/dist/core/keybindings.js
index 795c6a199bcb4cc88351be9bbef2e73cb8e4e61f..5969a652c15b161f187b088cf891b536c9145bf1 100644
--- a/dist/core/keybindings.js
+++ b/dist/core/keybindings.js
@@ -318,4 +318,9 @@ export class KeybindingsManager extends TuiKeybindingsManager {
         return toKeybindingsConfig(migrateKeybindingsConfig(rawConfig).config);
     }
 }
+// MIXCODE_KEYBINDINGS_EXPORT: keybindings.json loading with legacy-name
+// migration and string/string[] filtering, for hosts building their own managers.
+export function loadKeybindingsConfigFile(path) {
+    return KeybindingsManager.loadFromFile(path);
+}
 //# sourceMappingURL=keybindings.js.map
\ No newline at end of file
diff --git a/dist/core/session-manager.d.ts b/dist/core/session-manager.d.ts
index 76c53813b3c8de823afe3a34b4447da983450c26..7f23ccb04ef5dbd4ff42edd1f699767cf9865123 100644
--- a/dist/core/session-manager.d.ts
+++ b/dist/core/session-manager.d.ts
@@ -201,6 +201,8 @@ export declare function buildSessionProjection(entries: SessionEntry[], leafId?:
 /** Build the finalized model context from the canonical session projection. */
 export declare function buildSessionContext(entries: SessionEntry[], leafId?: string | null, byId?: Map<string, SessionEntry>): SessionContext;
 export declare function getDefaultSessionDir(cwd: string, agentDir?: string): string;
+/** MIXCODE_EXPORT: pure cwd->sessions-dir encoding (no mkdir side effect). */
+export declare function getDefaultSessionDirPath(cwd: string, agentDir?: string): string;
 /** Exported for testing */
 export declare function loadEntriesFromFile(filePath: string): FileEntry[];
 /** Exported for testing */
diff --git a/dist/core/session-manager.js b/dist/core/session-manager.js
index 0ddbb9cccaa7355ca44d4bbc768eaab8af57bac8..21fd635d73621893013b9fc8857b89f24fa545c9 100644
--- a/dist/core/session-manager.js
+++ b/dist/core/session-manager.js
@@ -287,7 +287,7 @@ export function buildSessionContext(entries, leafId, byId) {
  * Compute the default session directory for a cwd.
  * Encodes cwd into a safe directory name under ~/.pi/agent/sessions/.
  */
-function getDefaultSessionDirPath(cwd, agentDir = getDefaultAgentDir()) {
+export function getDefaultSessionDirPath(cwd, agentDir = getDefaultAgentDir()) {
     const resolvedCwd = resolvePath(cwd);
     const resolvedAgentDir = resolvePath(agentDir);
     const safePath = `--${resolvedCwd.replace(/^[/\\]/, "").replace(/[/\\:]/g, "-")}--`;
diff --git a/dist/core/settings-manager.js b/dist/core/settings-manager.js
index ed9d8d264da6f3315874bc909edeea0daef28b5a..256308eaa20f4b410c050872a63e82d62cdba673 100644
--- a/dist/core/settings-manager.js
+++ b/dist/core/settings-manager.js
@@ -639,9 +639,13 @@ export class SettingsManager {
         return mode !== undefined && CACHE_WARMING_MODES.includes(mode) ? mode : "streaming";
     }
     setCacheWarmingMode(mode) {
+        // MIXCODE_WARMING_OVERRIDES: changing this global-only mode must not
+        // discard a live session's unrelated applyOverrides(), such as context budgets.
+        const effectiveSettings = this.settings;
         this.globalSettings.cacheWarming = mode;
         this.markModified("cacheWarming");
         this.save();
+        this.settings = { ...effectiveSettings, cacheWarming: mode };
     }
     getProviderRetrySettings() {
         return {
diff --git a/dist/core/tools/edit-diff.d.ts b/dist/core/tools/edit-diff.d.ts
index 1778521de17b61e6c5ec9b9280e13ae59e3d61cc..ae22fc4875177225f5a34504f2cc3b6b066a0923 100644
--- a/dist/core/tools/edit-diff.d.ts
+++ b/dist/core/tools/edit-diff.d.ts
@@ -33,16 +33,13 @@ export declare function applyReplacementsPreservingUnchangedLines(originalConten
 export interface FuzzyMatchResult {
     /** Whether a match was found */
     found: boolean;
-    /** The index where the match starts (in the content that should be used for replacement) */
+    /** The index where the match starts in the original content */
     index: number;
-    /** Length of the matched text */
+    /** Length of the matched span in the original content */
     matchLength: number;
     /** Whether fuzzy matching was used (false = exact match) */
     usedFuzzyMatch: boolean;
-    /**
-     * The content to use for replacement operations.
-     * When exact match: original content. When fuzzy match: normalized content.
-     */
+    /** Original content for both exact and fuzzy replacements. */
     contentForReplacement: string;
 }
 export interface Edit {
@@ -55,19 +52,16 @@ export interface AppliedEditsResult {
 }
 /**
  * Find oldText in content, trying exact match first, then fuzzy match.
- * When fuzzy matching is used, the returned contentForReplacement is the
- * fuzzy-normalized version of the content (trailing whitespace stripped,
- * Unicode quotes/dashes normalized to ASCII).
+ * Both match coordinates and contentForReplacement refer to the original
+ * content. Normalization locates matches; it is never written to the file.
  */
 export declare function fuzzyFindText(content: string, oldText: string): FuzzyMatchResult;
 /**
  * Apply one or more exact-text replacements to LF-normalized content.
  *
- * All edits are matched against the same original content. Replacements are
- * then applied in reverse order so offsets remain stable. If any edit needs
- * fuzzy matching, the operation runs in fuzzy-normalized content space and then
- * overlays those line-level changes onto the original content so unchanged line
- * blocks keep their original bytes.
+ * All edits are matched against the same original content and applied in
+ * reverse order so offsets remain stable. Fuzzy matching only locates original
+ * spans; characters outside those spans are preserved, including on edited lines.
  */
 export declare function applyEditsToNormalizedContent(normalizedContent: string, edits: Edit[], path: string): AppliedEditsResult;
 /** Generate a standard unified patch. */
diff --git a/dist/core/tools/edit-diff.js b/dist/core/tools/edit-diff.js
index d1218b8c39248710d0c7acbbfcb686e4ff17f6bc..bad4b96deb7932a1063231659e28c724c3211f3c 100644
--- a/dist/core/tools/edit-diff.js
+++ b/dist/core/tools/edit-diff.js
@@ -132,11 +132,52 @@ export function applyReplacementsPreservingUnchangedLines(originalContent, baseC
     result += originalLines.slice(originalLineIndex).join("");
     return result;
 }
+const fuzzySegmenter = new Intl.Segmenter("und", { granularity: "grapheme" });
+/**
+ * Map a normalized column only at a complete grapheme boundary. Normalizing
+ * whole graphemes keeps combining sequences together and never splits a
+ * surrogate pair or an expanding compatibility character such as a ligature.
+ * A column inside an expansion has no safe original boundary.
+ */
+function mapFuzzyColumn(line, column) {
+    let normalizedColumn = 0;
+    for (const { segment, index } of fuzzySegmenter.segment(line)) {
+        if (normalizedColumn === column)
+            return index;
+        normalizedColumn += segment.normalize("NFKC").length;
+        if (normalizedColumn > column)
+            return undefined;
+    }
+    return normalizedColumn === column ? line.length : undefined;
+}
+/**
+ * Line breaks survive fuzzy normalization. At a trimmed line end, an end
+ * boundary excludes trailing whitespace; a start on the newline includes
+ * only the newline, leaving the preceding whitespace outside the edit.
+ */
+function mapFuzzyPosition(content, position, kind) {
+    let originalOffset = 0;
+    let normalizedOffset = 0;
+    const lines = content.split("\n");
+    for (let index = 0; index < lines.length; index++) {
+        const line = lines[index];
+        const normalizedEnd = normalizedOffset + normalizeForFuzzyMatch(line).length;
+        if (position < normalizedEnd ||
+            (position === normalizedEnd && (kind === "end" || index === lines.length - 1))) {
+            const column = mapFuzzyColumn(line, position - normalizedOffset);
+            return column === undefined ? undefined : originalOffset + column;
+        }
+        if (position === normalizedEnd)
+            return originalOffset + line.length;
+        originalOffset += line.length + 1;
+        normalizedOffset = normalizedEnd + 1;
+    }
+    return undefined;
+}
 /**
  * Find oldText in content, trying exact match first, then fuzzy match.
- * When fuzzy matching is used, the returned contentForReplacement is the
- * fuzzy-normalized version of the content (trailing whitespace stripped,
- * Unicode quotes/dashes normalized to ASCII).
+ * Both match coordinates and contentForReplacement refer to the original
+ * content. Normalization locates matches; it is never written to the file.
  */
 export function fuzzyFindText(content, oldText) {
     // Try exact match first
@@ -150,11 +191,16 @@ export function fuzzyFindText(content, oldText) {
             contentForReplacement: content,
         };
     }
-    // Try fuzzy match - work entirely in normalized space
+    // Locate in normalized space, then require safe original boundaries.
     const fuzzyContent = normalizeForFuzzyMatch(content);
     const fuzzyOldText = normalizeForFuzzyMatch(oldText);
-    const fuzzyIndex = fuzzyContent.indexOf(fuzzyOldText);
-    if (fuzzyIndex === -1) {
+    const fuzzyIndex = fuzzyOldText.length === 0 ? -1 : fuzzyContent.indexOf(fuzzyOldText);
+    const start = fuzzyIndex < 0 ? undefined : mapFuzzyPosition(content, fuzzyIndex, "start");
+    const end = fuzzyIndex < 0 ? undefined : mapFuzzyPosition(content, fuzzyIndex + fuzzyOldText.length, "end");
+    // Recheck the original span so a normalization boundary cannot widen the
+    // edit to characters the caller never named. Failed matches never write.
+    if (start === undefined || end === undefined ||
+        normalizeForFuzzyMatch(content.slice(start, end)) !== fuzzyOldText) {
         return {
             found: false,
             index: -1,
@@ -163,15 +209,12 @@ export function fuzzyFindText(content, oldText) {
             contentForReplacement: content,
         };
     }
-    // When fuzzy matching, return offsets in normalized space. Callers can use
-    // the normalized content to compute replacements, then decide how much of
-    // that normalized output should be written back.
     return {
         found: true,
-        index: fuzzyIndex,
-        matchLength: fuzzyOldText.length,
+        index: start,
+        matchLength: end - start,
         usedFuzzyMatch: true,
-        contentForReplacement: fuzzyContent,
+        contentForReplacement: content,
     };
 }
 function countOccurrences(content, oldText) {
@@ -206,11 +249,9 @@ function getNoChangeError(path, totalEdits) {
 /**
  * Apply one or more exact-text replacements to LF-normalized content.
  *
- * All edits are matched against the same original content. Replacements are
- * then applied in reverse order so offsets remain stable. If any edit needs
- * fuzzy matching, the operation runs in fuzzy-normalized content space and then
- * overlays those line-level changes onto the original content so unchanged line
- * blocks keep their original bytes.
+ * All edits are matched against the same original content and applied in
+ * reverse order so offsets remain stable. Fuzzy matching only locates original
+ * spans; characters outside those spans are preserved, including on edited lines.
  */
 export function applyEditsToNormalizedContent(normalizedContent, edits, path) {
     const normalizedEdits = edits.map((edit) => ({
@@ -222,17 +263,14 @@ export function applyEditsToNormalizedContent(normalizedContent, edits, path) {
             throw getEmptyOldTextError(path, i, normalizedEdits.length);
         }
     }
-    const initialMatches = normalizedEdits.map((edit) => fuzzyFindText(normalizedContent, edit.oldText));
-    const usedFuzzyMatch = initialMatches.some((match) => match.usedFuzzyMatch);
-    const replacementBaseContent = usedFuzzyMatch ? normalizeForFuzzyMatch(normalizedContent) : normalizedContent;
     const matchedEdits = [];
     for (let i = 0; i < normalizedEdits.length; i++) {
         const edit = normalizedEdits[i];
-        const matchResult = fuzzyFindText(replacementBaseContent, edit.oldText);
+        const matchResult = fuzzyFindText(normalizedContent, edit.oldText);
         if (!matchResult.found) {
             throw getNotFoundError(path, i, normalizedEdits.length);
         }
-        const occurrences = countOccurrences(replacementBaseContent, edit.oldText);
+        const occurrences = countOccurrences(normalizedContent, edit.oldText);
         if (occurrences > 1) {
             throw getDuplicateError(path, i, normalizedEdits.length, occurrences);
         }
@@ -252,9 +290,7 @@ export function applyEditsToNormalizedContent(normalizedContent, edits, path) {
         }
     }
     const baseContent = normalizedContent;
-    const newContent = usedFuzzyMatch
-        ? applyReplacementsPreservingUnchangedLines(normalizedContent, replacementBaseContent, matchedEdits)
-        : applyReplacements(replacementBaseContent, matchedEdits);
+    const newContent = applyReplacements(normalizedContent, matchedEdits);
     if (baseContent === newContent) {
         throw getNoChangeError(path, normalizedEdits.length);
     }
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 52dcaafdf0fac5b42a733039103edb68a7538de3..41f6d14bd08ff621b8284515d791c48b471d8ab2 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,6 +1,8 @@
 export { type Args, parseArgs } from "./cli/args.ts";
 export { CONFIG_DIR_NAME, getAgentDir, getDocsPath, getExamplesPath, getPackageDir, getReadmePath, VERSION, } from "./config.ts";
 export { AgentSession, type AgentSessionConfig, type AgentSessionEvent, type AgentSessionEventListener, type ModelCycleResult, type ParsedSkillBlock, type PromptOptions, parseSkillBlock, type SessionStats, } from "./core/agent-session.ts";
+export { formatCacheWarmingStatus } from "./core/cache-warmer.ts";
+export { CACHE_WARMING_MODES } from "./core/settings-manager.ts";
 export { readStoredCredential } from "./core/auth-storage.ts";
 export type { CacheWarmingDecision, CacheWarmingStatus } from "./core/cache-warmer.ts";
 export { type BranchPreparation, type BranchSummaryResult, type CollectEntriesResult, type CompactionResult, type CutPointResult, calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimateTokens, type FileOperations, findCutPoint, findTurnStartIndex, type GenerateBranchSummaryOptions, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, serializeConversation, shouldCompact, } from "./core/compaction/index.ts";
@@ -27,11 +29,44 @@ export { hasTrustRequiringProjectResources, type ProjectTrustDecision, ProjectTr
 export { type MainOptions, main } from "./main.ts";
 export { InteractiveMode, type InteractiveModeOptions, type JsonAgentSessionEvent, type ModelInfo, type PrintModeOptions, RpcClient, type RpcClientOptions, type RpcCommand, type RpcEventListener, type RpcExtensionUIRequest, type RpcExtensionUIResponse, type RpcResponse, type RpcSessionState, runPrintMode, runRpcMode, } from "./modes/index.ts";
 export { ArminComponent, AssistantMessageComponent, BashExecutionComponent, BorderedLoader, BranchSummaryMessageComponent, CompactionSummaryMessageComponent, CustomEditor, type CustomEditorOptions, CustomMessageComponent, DynamicBorder, ExtensionEditorComponent, ExtensionInputComponent, ExtensionSelectorComponent, FooterComponent, keyHint, keyText, LoginDialogComponent, ModelSelectorComponent, OAuthSelectorComponent, type RenderDiffOptions, rawKeyHint, renderDiff, SessionSelectorComponent, type SettingsCallbacks, type SettingsConfig, SettingsSelectorComponent, ShowImagesSelectorComponent, SkillInvocationMessageComponent, ThemeSelectorComponent, ThinkingSelectorComponent, ToolExecutionComponent, type ToolExecutionOptions, TreeSelectorComponent, truncateToVisualLines, UserMessageComponent, UserMessageSelectorComponent, type VisualTruncateResult, } from "./modes/interactive/components/index.ts";
-export { getLanguageFromPath, getMarkdownTheme, getSelectListTheme, getSettingsListTheme, highlightCode, initTheme, Theme, type ThemeColor, } from "./modes/interactive/theme/theme.ts";
+export {
+    Theme,
+    getAvailableThemes,
+    getLanguageFromPath,
+    getMarkdownTheme,
+    getSelectListTheme,
+    getSettingsListTheme,
+    getThemeByName,
+    highlightCode,
+    initTheme,
+    setRegisteredThemes,
+    setThemeInstance,
+    type ThemeColor,
+} from "./modes/interactive/theme/theme.ts";
+export { loadAllHighlightLanguages } from "./utils/syntax-highlight.ts";
 export { copyToClipboard } from "./utils/clipboard.ts";
 export { parseFrontmatter, stripFrontmatter } from "./utils/frontmatter.ts";
 export { convertToPng } from "./utils/image-convert.ts";
 export { formatDimensionNote, type ResizedImage, resizeImage } from "./utils/image-resize.ts";
 export { detectSupportedImageMimeTypeFromFile } from "./utils/mime.ts";
 export { getPowerShellConfig, getShellConfig } from "./utils/shell.ts";
+export { ensureTool } from "./utils/tools-manager.ts";
+export {
+    DEFAULT_HTTP_IDLE_TIMEOUT_MS,
+    HTTP_IDLE_TIMEOUT_CHOICES,
+    applyHttpProxySettings,
+    configureHttpDispatcher,
+    formatHttpIdleTimeoutMs,
+    parseHttpIdleTimeoutMs,
+} from "./core/http-dispatcher.ts";
+export { CACHE_TTL_MS, collectCacheMisses, computeCacheWaste, detectCacheMiss, type CacheMiss, type ModelPriceSource } from "./core/cache-stats.ts";
+export { getUsageCostBreakdown } from "./core/usage-totals.ts";
+export { buildSystemPrompt } from "./core/system-prompt.ts";
+export { FooterDataProvider } from "./core/footer-data-provider.ts";
+export { readClipboardText } from "./utils/clipboard.ts";
+export { extensionForImageMimeType, readClipboardImage } from "./utils/clipboard-image.ts";
+export { createMermaidMarkdownTransformer } from "./modes/interactive/components/mermaid.ts";
+export { loadKeybindingsConfigFile } from "./core/keybindings.ts";
+export { getDefaultSessionDirPath } from "./core/session-manager.ts";
+export { applyMarkdownTransformers } from "./modes/interactive/components/markdown-transform.ts";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index d8339661293e51358d0165680bad4334ff78f92e..5170f398b0f500abe3c181f4145071505e11aa74 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -3,6 +3,8 @@ export { parseArgs } from "./cli/args.js";
 // Config paths
 export { CONFIG_DIR_NAME, getAgentDir, getDocsPath, getExamplesPath, getPackageDir, getReadmePath, VERSION, } from "./config.js";
 export { AgentSession, parseSkillBlock, } from "./core/agent-session.js";
+export { formatCacheWarmingStatus } from "./core/cache-warmer.js";
+export { CACHE_WARMING_MODES } from "./core/settings-manager.js";
 export { readStoredCredential } from "./core/auth-storage.js";
 // Compaction
 export { calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimateTokens, findCutPoint, findTurnStartIndex, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, serializeConversation, shouldCompact, } from "./core/compaction/index.js";
@@ -36,7 +38,21 @@ export { InteractiveMode, RpcClient, runPrintMode, runRpcMode, } from "./modes/i
 // UI components for extensions
 export { ArminComponent, AssistantMessageComponent, BashExecutionComponent, BorderedLoader, BranchSummaryMessageComponent, CompactionSummaryMessageComponent, CustomEditor, CustomMessageComponent, DynamicBorder, ExtensionEditorComponent, ExtensionInputComponent, ExtensionSelectorComponent, FooterComponent, keyHint, keyText, LoginDialogComponent, ModelSelectorComponent, OAuthSelectorComponent, rawKeyHint, renderDiff, SessionSelectorComponent, SettingsSelectorComponent, ShowImagesSelectorComponent, SkillInvocationMessageComponent, ThemeSelectorComponent, ThinkingSelectorComponent, ToolExecutionComponent, TreeSelectorComponent, truncateToVisualLines, UserMessageComponent, UserMessageSelectorComponent, } from "./modes/interactive/components/index.js";
 // Theme utilities for custom tools and extensions
-export { getLanguageFromPath, getMarkdownTheme, getSelectListTheme, getSettingsListTheme, highlightCode, initTheme, Theme, } from "./modes/interactive/theme/theme.js";
+export {
+    Theme,
+    getAvailableThemes,
+    getLanguageFromPath,
+    getMarkdownTheme,
+    getSelectListTheme,
+    getSettingsListTheme,
+    getThemeByName,
+    highlightCode,
+    initTheme,
+    setRegisteredThemes,
+    setThemeInstance,
+} from "./modes/interactive/theme/theme.js";
+// Syntax highlighting: lazy full-language registration for markdown code fences
+export { loadAllHighlightLanguages } from "./utils/syntax-highlight.js";
 // Clipboard utilities
 export { copyToClipboard } from "./utils/clipboard.js";
 export { parseFrontmatter, stripFrontmatter } from "./utils/frontmatter.js";
@@ -45,4 +61,37 @@ export { formatDimensionNote, resizeImage } from "./utils/image-resize.js";
 export { detectSupportedImageMimeTypeFromFile } from "./utils/mime.js";
 // Shell utilities
 export { getPowerShellConfig, getShellConfig } from "./utils/shell.js";
+// Managed CLI tools
+export { ensureTool } from "./utils/tools-manager.js";
+// HTTP dispatcher (proxy + idle timeout); used by MixCode bootstrap
+export {
+    DEFAULT_HTTP_IDLE_TIMEOUT_MS,
+    HTTP_IDLE_TIMEOUT_CHOICES,
+    applyHttpProxySettings,
+    configureHttpDispatcher,
+    formatHttpIdleTimeoutMs,
+    parseHttpIdleTimeoutMs,
+} from "./core/http-dispatcher.js";
+// Cache waste stats; used by MixCode /session dump
+export {
+    CACHE_TTL_MS,
+    collectCacheMisses,
+    computeCacheWaste,
+    detectCacheMiss,
+} from "./core/cache-stats.js";
+// Usage cost breakdown; used by MixCode /session dump
+export { getUsageCostBreakdown } from "./core/usage-totals.js";
+// System prompt builder; used by MixCode system-prompt assembly
+export { buildSystemPrompt } from "./core/system-prompt.js";
+// Git branch footer data; used by MixCode chrome + extension footerData
+export { FooterDataProvider } from "./core/footer-data-provider.js";
+// Clipboard reads; used by MixCode editor paste
+export { readClipboardText } from "./utils/clipboard.js";
+export { extensionForImageMimeType, readClipboardImage } from "./utils/clipboard-image.js";
+// Mermaid markdown rendering; used by MixCode chat rendering
+export { createMermaidMarkdownTransformer } from "./modes/interactive/components/mermaid.js";
+// MIXCODE_EXPORTS: upstream helpers surfaced for the MixCode host.
+export { loadKeybindingsConfigFile } from "./core/keybindings.js";
+export { getDefaultSessionDirPath } from "./core/session-manager.js";
+export { applyMarkdownTransformers } from "./modes/interactive/components/markdown-transform.js";
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/modes/interactive/components/markdown-transform.d.ts b/dist/modes/interactive/components/markdown-transform.d.ts
index c819366a7a8f47b5cdb1aafb24bc1c813b7e1417..25c5fcb66f824d0b8101cf9d264c4dd27cc6b61f 100644
--- a/dist/modes/interactive/components/markdown-transform.d.ts
+++ b/dist/modes/interactive/components/markdown-transform.d.ts
@@ -1,3 +1,4 @@
 import type { MarkdownTransformContext, MarkdownTransformer } from "../../../core/extensions/types.ts";
 export declare function createMarkdownTransform(messageType: MarkdownTransformContext["messageType"], isStreaming: boolean, transformers: readonly MarkdownTransformer[]): (markdown: string, availableWidth: number) => string;
+export declare function applyMarkdownTransformers(markdown: string, context: MarkdownTransformContext, transformers: readonly MarkdownTransformer[]): string;
 //# sourceMappingURL=markdown-transform.d.ts.map
\ No newline at end of file
diff --git a/dist/modes/interactive/components/markdown-transform.js b/dist/modes/interactive/components/markdown-transform.js
index 5ac4c5670ab3bf0365535c3d078016a58babee49..7e9caa779193472582b1cd919c45a9edb5dfa8fd 100644
--- a/dist/modes/interactive/components/markdown-transform.js
+++ b/dist/modes/interactive/components/markdown-transform.js
@@ -1,7 +1,7 @@
 export function createMarkdownTransform(messageType, isStreaming, transformers) {
     return (markdown, availableWidth) => applyMarkdownTransformers(markdown, { messageType, isStreaming, availableWidth }, transformers);
 }
-function applyMarkdownTransformers(markdown, context, transformers) {
+export function applyMarkdownTransformers(markdown, context, transformers) {
     let transformedMarkdown = markdown;
     for (const transformer of transformers) {
         try {
diff --git a/dist/modes/interactive/components/mermaid.js b/dist/modes/interactive/components/mermaid.js
index 6015ae42db8b0a3571ac4c7748caa2f715b2ff44..8984f3cc8f89fc5805ccded2a3478f007ae59833 100644
--- a/dist/modes/interactive/components/mermaid.js
+++ b/dist/modes/interactive/components/mermaid.js
@@ -51,6 +51,11 @@ export function createMermaidMarkdownTransformer(options) {
             (context.isStreaming && mode !== "streaming")) {
             return markdown;
         }
+        // Unfenced messages cannot contain Mermaid blocks. Preserve their source
+        // and avoid a full Markdown parse before the renderer parses it again.
+        if (!markdown.includes("```") && !markdown.includes("~~~")) {
+            return markdown;
+        }
         return markdownParser
             .lexer(markdown)
             .map((token) => {
